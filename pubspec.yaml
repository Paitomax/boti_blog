name: botiblog
description: Veja atualizações dos colegas e também do grupo Boticário.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  dio: ^3.0.9
  bloc: ^4.0.0
  flutter_bloc: ^4.0.0
  json_annotation: ^3.0.1
  sqflite: ^1.3.0+2
  hash: ^1.0.1
  equatable: ^1.2.0
  intl: ^0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.0.0
  json_serializable: ^3.3.0

flutter:
  uses-material-design: true